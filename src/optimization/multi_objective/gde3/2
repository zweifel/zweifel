
pdf("rose.pdf")

require(CircStats)

k <- read.table("all_evaluations")

k <- k[ k[,1] != -999, ]

print(k)
print(a)

b <- a[,1]
a <- a[,2]

rose.diag(atan2(a,b), bins = 18, main = 'Stacked Points', pts=TRUE)

dev.off()
