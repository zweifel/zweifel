

#include"stdlib.h"
#include"stdio.h"
#include"math.h"

float entropy(int *variable, int variable_size, int** sample, int sample_size);

double euclidianDistance(double* a, double* b, int length);

double chebyshevDistance(int x, int y, int x2, int y2);
